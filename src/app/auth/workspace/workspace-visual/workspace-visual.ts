import { Component } from '@angular/core';

@Component({
    selector: 'app-workspace-visual',
    standalone: true,
    templateUrl: './workspace-visual.html',
    styleUrl: './workspace-visual.scss',
})
export class WorkspaceVisualComponent { }
